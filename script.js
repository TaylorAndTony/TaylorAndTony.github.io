let githubPage = 'https://github.com/TaylorAndTony';
let biliPage = 'https://space.bilibili.com/47256272';

function toUrl(url) {
    window.open(url)
}

function jumpMine() {
    toUrl(githubPage)
}

function jumpBili() {
    toUrl(biliPage)
}